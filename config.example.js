// Copy this file to `config.js` and fill in your own API key locally.
// IMPORTANT: If this is a purely static site served to browsers, any API key
// placed here can be extracted by users from DevTools. For production, proxy
// requests through your own backend.
window.CONFIG = {
  DEEPSEEK_API_KEY: "sk-REPLACE_ME",
  // DeepSeek provides an OpenAI-compatible Chat Completions API.
  DEEPSEEK_API_URL: "https://api.deepseek.com/chat/completions",
  DEEPSEEK_MODEL: "deepseek-chat",

  GYM_NAME: "活力健身馆",
  GYM_LOCATION: "香港大学"
};

